node_modules/
dist/
.DS_Store
.env
.env.*
!.env.example
!.env.saas.example

# PocketBase
pocketbase/pb_data/
pocketbase/pocketbase
pocketbase/pb_public/

# iOS / Xcode
ios-app/DerivedData/
ios-app/*.xcworkspace/xcuserdata/
ios-app/*.xcodeproj/xcuserdata/
ios-app/.build/
**/*.xcuserstate

# Claude Code / Superpowers
.claude/
.superpowers/
docs/superpowers/
.worktrees/

# Design-Dateien
*.af
*.png
*.jpg
!webapp-mockup.png

# Interne Entwicklungsdokumente (Verzeichnisinhalt, aber Verzeichnis selbst begehbar)
docs/*
web-app/docs/
# Ausnahme: Betriebs-Doku (für Deployment gedacht, nicht intern)
!docs/saas-betrieb.md
!docs/deploy-cx43.md
!docs/files.md

# Heroku / sonstige Deployment-Artefakte
Procfile

# (Removed root package.json ignore since we are now a monorepo)

# Local dev scripts
start.sh
ecosystem.config.cjs
Dev-Server-App/
DEV-SERVER.md

# Laufzeit-Daten
data/

# Sicherheits-Audits: listen offene Schwachstellen mit Fundstellen.
# Gehören nicht in ein öffentliches Repo, solange die Findings offen sind.
# Negation-Patterns wirken nicht, wenn das Verzeichnis selbst per "audits/"
# ausgeschlossen wird (Git traversiert dann gar nicht erst hinein) — daher
# hier gezielt nur die Berichtsdateien ignorieren, nicht den ganzen Ordner.
audits/*.md
!audits/README.md
!audits/backlog.md
audits_old/

# In-App-Updater: temporäre Backup-/Testdateien (siehe server/routes/update.js)
tmp-release.zip
.update-backup/
.update-smoketest-data/

# Logs
*.log
**/.server.log

# Playwright-Testartefakte
test-results/
playwright-report/

# iOS Build-Artefakte
ios-app/**/*.xcarchive
ios-app/Pods/

.mcp.json
